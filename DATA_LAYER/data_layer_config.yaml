# NoTrade Data Layer — legacy mirror (deprecated / NON-SSOT for new runtime code)
#
# New executable code MUST read from `DATA_LAYER/NOTRADE_CONSTANTS.yaml` only.
# Keep this file temporarily only for backward compatibility with older scripts.

# NoTrade Data Layer — legacy convenience config
#
# Prefer using `NOTRADE_CONSTANTS.yaml` (section: data_layer)
# This file exists so older scripts can still read flat keys.

EDGE_NET_THRESHOLD: 0.03

POLY_MAX_STALENESS_S: 10
REF_MAX_STALENESS_S: 30

MAX_SPREAD_PCT: 0.02

PROB_JUMP_ALERT: 0.12
PROB_JUMP_WINDOW_S: 180

# Probability model cadence (legacy convenience mirror; prefer NOTRADE_CONSTANTS.yaml)
PREDICTION_CADENCE_MIN: 30
